name: Manual Deploy (POC)

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version being deployed (e.g. 1.1.3) — must match an existing cloud-settings-* tag'
        required: true
        type: string

permissions:
  contents: write
  pull-requests: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # Step A: find the currently-open draft release and snapshot its body.
      - name: "Step A - Locate active draft release"
        id: draft
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          DRAFT_JSON=$(gh api "repos/${{ github.repository }}/releases" --paginate \
            --jq '[.[] | select(.draft == true)][0]')

          if [ -z "$DRAFT_JSON" ] || [ "$DRAFT_JSON" == "null" ]; then
            echo "::error::No open draft release found. Has release-drafter run yet on main?"
            exit 1
          fi

          DRAFT_ID=$(echo "$DRAFT_JSON" | jq -r '.id')
          DRAFT_TAG=$(echo "$DRAFT_JSON" | jq -r '.tag_name')
          DRAFT_BODY=$(echo "$DRAFT_JSON" | jq -r '.body')

          echo "Found draft id=$DRAFT_ID currently tracking tag=$DRAFT_TAG"

          echo "draft_id=$DRAFT_ID" >> "$GITHUB_OUTPUT"
          {
            echo "draft_body<<DRAFT_BODY_EOF"
            echo "$DRAFT_BODY"
            echo "DRAFT_BODY_EOF"
          } >> "$GITHUB_OUTPUT"

      # Step B: simulate a region-by-region deploy and PATCH the rollout
      # status straight into the current draft body via the REST API.
      - name: "Step B - Simulate deploy, PATCH rollout status onto the draft"
        id: rollout
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          DRAFT_ID: ${{ steps.draft.outputs.draft_id }}
          DRAFT_BODY: ${{ steps.draft.outputs.draft_body }}
        run: |
          DEPLOY_TIME=$(date -u +"%Y-%m-%dT%H:%M:%SZ")

          ROLLOUT_TABLE=$(cat <<EOF

          ## 🚀 Rollout Status

          | Region      | Status     | Timestamp (UTC)     |
          |-------------|------------|----------------------|
          | us-east-1   | ✅ Success | ${DEPLOY_TIME}       |
          | us-west-2   | ✅ Success | ${DEPLOY_TIME}       |
          | eu-west-1   | ✅ Success | ${DEPLOY_TIME}       |

          _Deployed version: ${{ inputs.version }}_
          EOF
          )

          # Save the table on its own so Step D can re-attach it as a footer
          # after release-drafter regenerates the body (see README for why).
          {
            echo "rollout_table<<ROLLOUT_EOF"
            echo "$ROLLOUT_TABLE"
            echo "ROLLOUT_EOF"
          } >> "$GITHUB_OUTPUT"

          NEW_BODY="${DRAFT_BODY}${ROLLOUT_TABLE}"

          jq -n --arg body "$NEW_BODY" '{body: $body}' | gh api \
            --method PATCH \
            "repos/${{ github.repository }}/releases/${DRAFT_ID}" \
            --input -

          echo "Patched draft ${DRAFT_ID} with rollout status for regions."

      # Step C: sleep, giving you a window to merge a competing PR to main
      # (simulating the 1.1.4 race) while this deploy is still "in flight".
      - name: "Step C - Sleep to allow race-condition simulation"
        run: |
          echo "Sleeping 15s. Merge a competing PR to main NOW to simulate the race condition..."
          sleep 15

      # Step D: publish the release for the tag we were actually deploying,
      # not whatever main/the draft looks like right now. Because the tag
      # cloud-settings-${{ inputs.version }} was created by auto-tag.yml at
      # merge time (constraint #1), it's pinned to a specific commit SHA,
      # so release-drafter computes the changelog only up to that commit —
      # this is what makes the race self-heal.
      #
      # `footer` re-attaches the rollout table Step B computed, because
      # release-drafter fully regenerates the release body from the template
      # on every run (it does NOT preserve manual PATCHes).
      - name: "Step D - Publish the release for this version"
        uses: release-drafter/release-drafter@v6
        with:
          publish: true
          version: ${{ inputs.version }}
          tag: cloud-settings-${{ inputs.version }}
          footer: ${{ steps.rollout.outputs.rollout_table }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # Step E: immediately force release-drafter to regenerate a fresh
      # draft for whatever is left unreleased on main (e.g. the 1.1.4 PR
      # that snuck in during Step C), instead of waiting for the next push.
      # This is what makes "a brand new clean draft appears right after
      # publish" happen deterministically for the POC, rather than by luck.
      - name: "Step E - Recreate the next draft immediately"
        uses: release-drafter/release-drafter@v6
        with:
          publish: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
